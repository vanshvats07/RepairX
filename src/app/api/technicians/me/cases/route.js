import { NextResponse } from "next/server";
import { connectMongo } from "@/lib/mongodb";
import Technician from "@/models/Technician";
import TechnicianAssignment from "@/models/TechnicianAssignment";
import { requireAuth, requireRole } from "@/services/authService";
export async function GET() { try { const user = requireRole(await requireAuth(), ["TECHNICIAN"]); await connectMongo(); if (!process.env.MONGODB_URI) return NextResponse.json({ data: [] }); const technician = await Technician.findOne({ userId: user._id, status: "ACTIVE" }); if (!technician) return NextResponse.json({ data: [] }); const data = await TechnicianAssignment.find({ technicianId: technician._id, workshopId: technician.workshopId, status: { $in: ["ASSIGNED", "ACCEPTED"] } }).populate({ path: "repairRequestId", populate: [{ path: "deviceId", select: "brand model variant storage location" }, { path: "workshopId", select: "name city locality" }, { path: "investigationId", select: "assessmentState structuredAssessment possibleCauses recommendedChecks aiConfidence" }] }).lean(); return NextResponse.json({ data }); } catch (error) { return NextResponse.json({ error: error.message }, { status: error.code === "FORBIDDEN" ? 403 : 401 }); } }
